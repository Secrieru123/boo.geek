let randCoord = (min,max) => {
    return Math.floor(min + (max - min) * Math.random())
}

let randDelay = (delay) => {
    return Math.floor(delay  * Math.random())
}


let randFloat = (min,max) => {
    return min + (max-min)  * Math.random()
}

