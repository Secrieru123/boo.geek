// DECLARATION FUNCTIONS

// 1. old school function (hoisting) 
function someName (arg1, arg2, etc) {
    int1
    inst2
    isNaN
    return value
}

// 2. functional expressions 
const someName = function (arg1, arg2, etc) {
    int1
    inst2
    isNaN
    return value
}


//3. arrow functions

const someName = (arg1, arg2, etc) => {
    int1
    inst2
    isNaN
    return value
}